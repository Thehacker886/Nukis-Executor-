-- Nukis Executor - Interface moderna com sistema de abas
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local mouse = player:GetMouse()

-- UI principal
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "NukisExecutor"

-- Botão Open com aura roxa
local OpenButton = Instance.new("TextButton", ScreenGui)
OpenButton.Size = UDim2.new(0, 80, 0, 80)
OpenButton.Position = UDim2.new(0, 50, 0.5, -40)
OpenButton.BackgroundColor3 = Color3.fromRGB(128, 0, 128)
OpenButton.Text = "Open"
OpenButton.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenButton.TextSize = 16
OpenButton.Font = Enum.Font.GothamBold
OpenButton.Active = true
OpenButton.Draggable = true
OpenButton.Name = "OpenButton"

-- Fazer o botão redondo
local OpenUICorner = Instance.new("UICorner", OpenButton)
OpenUICorner.CornerRadius = UDim.new(0.5, 0)

-- Efeito de aura roxa (sombra externa)
local AuraFrame = Instance.new("Frame", ScreenGui)
AuraFrame.Size = UDim2.new(0, 100, 0, 100)
AuraFrame.Position = UDim2.new(0, 40, 0.5, -50)
AuraFrame.BackgroundColor3 = Color3.fromRGB(128, 0, 128)
AuraFrame.BackgroundTransparency = 0.7
AuraFrame.BorderSizePixel = 0
AuraFrame.ZIndex = 0

local AuraUICorner = Instance.new("UICorner", AuraFrame)
AuraUICorner.CornerRadius = UDim.new(0.5, 0)

-- Sincronizar posição da aura com o botão
local function updateAuraPosition()
    AuraFrame.Position = UDim2.new(0, OpenButton.AbsolutePosition.X - 10, 0, OpenButton.AbsolutePosition.Y - 10)
end

OpenButton:GetPropertyChangedSignal("AbsolutePosition"):Connect(updateAuraPosition)
updateAuraPosition()

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 600, 0, 400)
MainFrame.Position = UDim2.new(0.5, -300, 0.5, -200)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Name = "MainFrame"
MainFrame.Visible = false

-- Cantos arredondados
local UICorner = Instance.new("UICorner", MainFrame)
UICorner.CornerRadius = UDim.new(0, 10)

-- Título
local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundTransparency = 1
Title.Text = "Nukis Executor"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 24
Title.Font = Enum.Font.GothamBold
Title.Name = "Title"

-- Separador
local Line = Instance.new("Frame", MainFrame)
Line.Size = UDim2.new(1, -20, 0, 1)
Line.Position = UDim2.new(0, 10, 0, 40)
Line.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
Line.BorderSizePixel = 0

-- Botão Close
local CloseBtn = Instance.new("TextButton", MainFrame)
CloseBtn.Size = UDim2.new(0, 25, 0, 25)
CloseBtn.Position = UDim2.new(1, -35, 0, 7.5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Name = "CloseBtn"

local CloseUICorner = Instance.new("UICorner", CloseBtn)
CloseUICorner.CornerRadius = UDim.new(0, 4)

-- Container das abas laterais
local TabContainer = Instance.new("Frame", MainFrame)
TabContainer.Size = UDim2.new(0, 150, 1, -50)
TabContainer.Position = UDim2.new(0, 10, 0, 45)
TabContainer.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TabContainer.BorderSizePixel = 0

local TabUICorner = Instance.new("UICorner", TabContainer)
TabUICorner.CornerRadius = UDim.new(0, 8)

-- Container do conteúdo
local ContentContainer = Instance.new("Frame", MainFrame)
ContentContainer.Size = UDim2.new(0, 420, 1, -50)
ContentContainer.Position = UDim2.new(0, 170, 0, 45)
ContentContainer.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
ContentContainer.BorderSizePixel = 0

local ContentUICorner = Instance.new("UICorner", ContentContainer)
ContentUICorner.CornerRadius = UDim.new(0, 8)

-- Sistema de abas
local tabs = {}
local currentTab = nil

-- Função para criar aba
local function createTab(name, icon, yPos)
    local tabBtn = Instance.new("TextButton", TabContainer)
    tabBtn.Size = UDim2.new(1, -10, 0, 40)
    tabBtn.Position = UDim2.new(0, 5, 0, yPos)
    tabBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    tabBtn.Text = "  " .. icon .. "  " .. name
    tabBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
    tabBtn.TextSize = 14
    tabBtn.Font = Enum.Font.Gotham
    tabBtn.TextXAlignment = Enum.TextXAlignment.Left
    
    local tabUICorner = Instance.new("UICorner", tabBtn)
    tabUICorner.CornerRadius = UDim.new(0, 6)
    
    -- Container do conteúdo da aba
    local content = Instance.new("Frame", ContentContainer)
    content.Size = UDim2.new(1, -10, 1, -10)
    content.Position = UDim2.new(0, 5, 0, 5)
    content.BackgroundTransparency = 1
    content.Visible = false
    
    tabs[name] = {
        button = tabBtn,
        content = content
    }
    
    tabBtn.MouseButton1Click:Connect(function()
        switchTab(name)
    end)
    
    return content
end

-- Função para trocar de aba
function switchTab(tabName)
    -- Desativar aba atual
    if currentTab then
        tabs[currentTab].button.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
        tabs[currentTab].button.TextColor3 = Color3.fromRGB(200, 200, 200)
        tabs[currentTab].content.Visible = false
    end
    
    -- Ativar nova aba
    currentTab = tabName
    tabs[tabName].button.BackgroundColor3 = Color3.fromRGB(70, 130, 200)
    tabs[tabName].button.TextColor3 = Color3.fromRGB(255, 255, 255)
    tabs[tabName].content.Visible = true
end

-- Criar abas
local playerContent = createTab("Player", "👤", 10)
local executorContent = createTab("Executor", "⚡", 60)

-- CONTEÚDO DA ABA PLAYER
local playerTitle = Instance.new("TextLabel", playerContent)
playerTitle.Size = UDim2.new(1, 0, 0, 30)
playerTitle.BackgroundTransparency = 1
playerTitle.Text = "👤 Player Settings"
playerTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
playerTitle.TextSize = 18
playerTitle.Font = Enum.Font.GothamBold
playerTitle.TextXAlignment = Enum.TextXAlignment.Left

-- Função para criar slider
local function createSlider(parent, name, minVal, maxVal, defaultVal, yPos, callback)
    local sliderFrame = Instance.new("Frame", parent)
    sliderFrame.Size = UDim2.new(1, -20, 0, 60)
    sliderFrame.Position = UDim2.new(0, 10, 0, yPos)
    sliderFrame.BackgroundTransparency = 1
    
    local label = Instance.new("TextLabel", sliderFrame)
    label.Size = UDim2.new(1, 0, 0, 20)
    label.BackgroundTransparency = 1
    label.Text = name .. ": " .. defaultVal
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextSize = 14
    label.Font = Enum.Font.Gotham
    label.TextXAlignment = Enum.TextXAlignment.Left
    
    local sliderBg = Instance.new("Frame", sliderFrame)
    sliderBg.Size = UDim2.new(1, 0, 0, 8)
    sliderBg.Position = UDim2.new(0, 0, 0, 25)
    sliderBg.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    sliderBg.BorderSizePixel = 0
    
    local sliderBgCorner = Instance.new("UICorner", sliderBg)
    sliderBgCorner.CornerRadius = UDim.new(0, 4)
    
    local sliderFill = Instance.new("Frame", sliderBg)
    sliderFill.Size = UDim2.new(defaultVal/maxVal, 0, 1, 0)
    sliderFill.BackgroundColor3 = Color3.fromRGB(70, 130, 200)
    sliderFill.BorderSizePixel = 0
    
    local sliderFillCorner = Instance.new("UICorner", sliderFill)
    sliderFillCorner.CornerRadius = UDim.new(0, 4)
    
    local sliderButton = Instance.new("TextButton", sliderBg)
    sliderButton.Size = UDim2.new(0, 16, 0, 16)
    sliderButton.Position = UDim2.new(defaultVal/maxVal, -8, 0.5, -8)
    sliderButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    sliderButton.Text = ""
    sliderButton.BorderSizePixel = 0
    
    local sliderButtonCorner = Instance.new("UICorner", sliderButton)
    sliderButtonCorner.CornerRadius = UDim.new(0.5, 0)
    
    local dragging = false
    
    sliderButton.MouseButton1Down:Connect(function()
        dragging = true
    end)
    
    mouse.Button1Up:Connect(function()
        dragging = false
    end)
    
    mouse.Move:Connect(function()
        if dragging then
            local relativeX = mouse.X - sliderBg.AbsolutePosition.X
            local percentage = math.clamp(relativeX / sliderBg.AbsoluteSize.X, 0, 1)
            local value = math.floor(minVal + (maxVal - minVal) * percentage)
            
            sliderButton.Position = UDim2.new(percentage, -8, 0.5, -8)
            sliderFill.Size = UDim2.new(percentage, 0, 1, 0)
            label.Text = name .. ": " .. value
            
            callback(value)
        end
    end)
    
    return sliderFrame
end

-- Slider de WalkSpeed
createSlider(playerContent, "WalkSpeed", 16, 200, 16, 40, function(value)
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        player.Character.Humanoid.WalkSpeed = value
    end
end)

-- Slider de JumpPower
createSlider(playerContent, "JumpPower", 50, 200, 50, 110, function(value)
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        player.Character.Humanoid.JumpPower = value
    end
end)

-- CONTEÚDO DA ABA EXECUTOR
local executorTitle = Instance.new("TextLabel", executorContent)
executorTitle.Size = UDim2.new(1, 0, 0, 30)
executorTitle.BackgroundTransparency = 1
executorTitle.Text = "⚡ Script Executor"
executorTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
executorTitle.TextSize = 18
executorTitle.Font = Enum.Font.GothamBold
executorTitle.TextXAlignment = Enum.TextXAlignment.Left

-- ScrollingFrame para o código
local ScrollFrame = Instance.new("ScrollingFrame", executorContent)
ScrollFrame.Size = UDim2.new(1, -20, 0, 240)
ScrollFrame.Position = UDim2.new(0, 10, 0, 40)
ScrollFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
ScrollFrame.BorderSizePixel = 0
ScrollFrame.ScrollBarThickness = 6
ScrollFrame.ScrollBarImageColor3 = Color3.fromRGB(100, 100, 100)

local ScrollUICorner = Instance.new("UICorner", ScrollFrame)
ScrollUICorner.CornerRadius = UDim.new(0, 8)

-- TextBox para código
local CodeBox = Instance.new("TextBox", ScrollFrame)
CodeBox.Size = UDim2.new(1, -10, 1, 0)
CodeBox.Position = UDim2.new(0, 5, 0, 0)
CodeBox.BackgroundTransparency = 1
CodeBox.Text = ""
CodeBox.PlaceholderText = "-- Digite seu código Lua aqui\nprint('Hello World!')"
CodeBox.TextColor3 = Color3.fromRGB(255, 255, 255)
CodeBox.TextSize = 14
CodeBox.Font = Enum.Font.Code
CodeBox.ClearTextOnFocus = false
CodeBox.MultiLine = true
CodeBox.TextXAlignment = Enum.TextXAlignment.Left
CodeBox.TextYAlignment = Enum.TextYAlignment.Top
CodeBox.AutomaticSize = Enum.AutomaticSize.Y

-- Botões do Executor
local ExecuteBtn = Instance.new("TextButton", executorContent)
ExecuteBtn.Size = UDim2.new(0, 100, 0, 35)
ExecuteBtn.Position = UDim2.new(0, 10, 1, -45)
ExecuteBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 0)
ExecuteBtn.Text = "Execute"
ExecuteBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ExecuteBtn.TextSize = 16
ExecuteBtn.Font = Enum.Font.GothamBold

local ExecuteUICorner = Instance.new("UICorner", ExecuteBtn)
ExecuteUICorner.CornerRadius = UDim.new(0, 8)

local ClearBtn = Instance.new("TextButton", executorContent)
ClearBtn.Size = UDim2.new(0, 100, 0, 35)
ClearBtn.Position = UDim2.new(0, 120, 1, -45)
ClearBtn.BackgroundColor3 = Color3.fromRGB(120, 0, 0)
ClearBtn.Text = "Clear"
ClearBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ClearBtn.TextSize = 16
ClearBtn.Font = Enum.Font.GothamBold

local ClearUICorner = Instance.new("UICorner", ClearBtn)
ClearUICorner.CornerRadius = UDim.new(0, 8)

-- Funcionalidades
ExecuteBtn.MouseButton1Click:Connect(function()
    local code = CodeBox.Text
    if code and code ~= "" then
        local success, error = pcall(function()
            loadstring(code)()
        end)
        if not success then
            warn("Nukis Executor Error: " .. tostring(error))
        end
    end
end)

ClearBtn.MouseButton1Click:Connect(function()
    CodeBox.Text = ""
end)

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    OpenButton.Visible = true
    AuraFrame.Visible = true
end)

-- Funcionalidade do botão Open
local isOpen = false

OpenButton.MouseButton1Click:Connect(function()
    isOpen = not isOpen
    if isOpen then
        MainFrame.Visible = true
        OpenButton.Visible = false
        AuraFrame.Visible = false
        OpenButton.Text = "Close"
    else
        MainFrame.Visible = false
        OpenButton.Visible = true
        AuraFrame.Visible = true
        OpenButton.Text = "Open"
    end
end)

-- Efeito de pulsação na aura
spawn(function()
    while true do
        if AuraFrame.Visible then
            -- Expandir
            game:GetService("TweenService"):Create(
                AuraFrame,
                TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
                {Size = UDim2.new(0, 110, 0, 110), BackgroundTransparency = 0.8}
            ):Play()
            
            wait(1)
            
            -- Contrair
            game:GetService("TweenService"):Create(
                AuraFrame,
                TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
                {Size = UDim2.new(0, 100, 0, 100), BackgroundTransparency = 0.7}
            ):Play()
            
            wait(1)
        else
            wait(0.1)
        end
    end
end)

-- Efeitos visuais
local function addHoverEffect(button, normalColor, hoverColor)
    button.MouseEnter:Connect(function()
        button.BackgroundColor3 = hoverColor
    end)
    
    button.MouseLeave:Connect(function()
        button.BackgroundColor3 = normalColor
    end)
end

addHoverEffect(ExecuteBtn, Color3.fromRGB(0, 120, 0), Color3.fromRGB(0, 150, 0))
addHoverEffect(ClearBtn, Color3.fromRGB(120, 0, 0), Color3.fromRGB(150, 0, 0))
addHoverEffect(CloseBtn, Color3.fromRGB(200, 0, 0), Color3.fromRGB(220, 0, 0))
addHoverEffect(OpenButton, Color3.fromRGB(128, 0, 128), Color3.fromRGB(148, 0, 148))

-- Inicializar primeira aba
switchTab("Player")

print("Nukis Executor carregado com sucesso!")
